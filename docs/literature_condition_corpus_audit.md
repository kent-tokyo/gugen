# Literature condition corpus audit (Phase 20A)

A full-corpus inventory of the Kononova et al. 2019 text-mined synthesis dataset -- the same corpus `benchmarks/fetch_kononova.py` already draws its Phase 11 holdout benchmark sample from -- run over all raw records, not a downsampled subset. Produced by `benchmarks/audit_kononova_corpus.py`, regenerated by re-running that script, not hand-edited.

**Report-only.** No provider code, no Planner connection. This exists to decide whether building a bulk literature-condition provider on this corpus is worthwhile, independent of and prior to Phase 19P's thermodynamic work.

## Source

Kononova, O., Huo, H., He, T., Rong, Z., Botari, T., Sun, W., Tshitoyan, V., Ceder, G. "Text-mined dataset of inorganic materials synthesis recipes." *Scientific Data* 6, 203 (2019).

Hosted at https://api.figshare.com/v2/articles/9722159 (DOI 10.6084/m9.figshare.9722159), license **CC BY 4.0**, verified live against the figshare API by this script on every run (not reused from any prior cache -- see this project's own Phase 11 incident, `CHANGELOG.md`, for why that matters). Raw record count: 19488 (matches `fetch_kononova.py`'s independently verified count).

## Structural validity

- Raw records: 19488
- Structurally valid (target and every precursor parseable, 1-4 distinct precursors -- same bar `fetch_kononova.py` uses): 9045 (46.4%)
- Parse failures: 10443 (53.6%), broken down as:
  - unparseable target (doped/disordered/free-variable formula): 9081
  - unparseable precursor (same criteria): 954
  - zero precursors, or more than 4 after de-duplication: 408

## Coverage (breadth)

- Unique targets (by canonical elemental ratio, not formula string): 4631
- Unique exact routes (target + precursor-set, by canonical ratio): 5631
- Materials appearing only as a target: 5040
- Materials appearing only as a precursor: 815
- Materials appearing as both a target (in one record) and a precursor (in another): 79
- DOI coverage: 9045/9045 (100.0%)
- License coverage: 100% -- one figshare deposit, one corpus-level CC BY 4.0 license; there is no per-record license variation to measure.

## Condition-field coverage (per valid record)

Whether a record has *any* `HeatingOperation` reporting each field -- not whether every heating step does. gugen's `ConditionPrecedent` needs a resolved value per field per purpose (AGENTS.md's Heat-step model), so this is an upper bound on what a bulk provider could resolve, not the provider's actual per-purpose yield.

- Has at least one HeatingOperation at all: 7364 (81.4%)
- Temperature resolved (>=1 heating step with a numeric value): 7227 (79.9%)
- Duration resolved: 6428 (71.1%)
- Atmosphere reported (non-empty list): 4050 (44.8%)
- Ramp rate: **0%, structurally absent.** Verified directly against the schema, not inferred: every `conditions` object in this corpus (across all operation types) exposes only `{atmosphere, heating_temperature, heating_time, mixing_device, mixing_media}` -- there is no ramp-rate key anywhere in the raw data.
- Pressure (e.g. uniaxial pressing pressure): **0%, structurally absent** for the same reason -- `ShapingOperation` entries use the identical `conditions` shape as `HeatingOperation` (and rarely populate it), with no pressure-specific field.
- Milling conditions: no dedicated field, but `mixing_device`/`mixing_media` are populated and carry real content -- not a structured milling speed/duration/ball-to-powder ratio, but free-text material and solvent mentions (`mixing_device` values are dominated by grinding-vessel material, e.g. `agate`, `zirconia`, `alumina`; `mixing_media` values are dominated by wet-grinding solvents, e.g. `ethanol`, `water`, `acetone`). Record-level coverage: `mixing_device` in >=1 operation: 2390 (26.4%); `mixing_media`: 2632 (29.1%). Neither maps onto a gugen `ConditionPrecedent` field today -- gugen's `Grind`/`Form` steps have no vessel-material or solvent field to receive this.

## Route-family shape

gugen offers `ConventionalSolidState` and `Mechanochemical` unconditionally for every accepted precursor set (Phase 12, AGENTS.md §13) -- this corpus's records don't carry an explicit route-family label to match against. Operation-type frequency across the full raw corpus (all records, all operations):

- `Mixing`: 32035
- `HeatingOperation`: 31840
- `ShapingOperation`: 11225
- `DryingOperation`: 7017
- `LiquidGrinding`: 5861
- `QuenchingOperation`: 5659
- `SolutionMixing`: 3427

**No `BallMilling`-shaped operation type exists in this corpus's vocabulary.** The closest candidate, `LiquidGrinding`, describes wet grinding (its `mixing_media` values are dominated by solvents -- ethanol, water, acetone) as a mixing step, not the single high-energy dry ball-milling step gugen's `mechanochemical_template` models (Suryanarayana 2001, `10.1016/S0079-6425(99)00010-9`). Only 241 of 19488 raw records (1.2%) have a `LiquidGrinding` step and *no* `HeatingOperation` at all -- the closest this corpus comes to a grinding-only route -- and even those are wet grinding, not dry ball milling. This corpus is a source of conventional solid-state condition precedents (mix/heat cycles); it does not evidence the Mechanochemical route family in any meaningful way -- a real scope limit, not something a classification heuristic on this data could fix.

## Duplication and conflict

- Exact-duplicate records (same route, same DOI, same reported max temperature set -- almost certainly the same underlying report entered twice): 19 records across 19 groups (the counts are equal, meaning every duplicate group has exactly one extra copy -- no group of 3 or more was found).
- Routes independently reported by >=2 distinct DOIs: 1056 of 5631 unique routes.
- Of those multi-DOI routes, the fraction where reported maximum heating temperatures differ across sources: 700/1056 (66.3%).

**This is an upper bound on per-purpose conflict, not a measurement of it, and should not be read as "66.3% of routes genuinely disagree."** The metric compares each record's own single highest reported heating temperature, grouped by canonical target+precursor-set ratio -- it has no `HeatingPurpose` label to match against, because this corpus doesn't carry one. A difference here can also come from one paper reporting calcination+sintering while another reports sintering only (different step counts, not disagreement), or from the ratio match unifying two genuinely different recipes at the same stoichiometry. What this number does establish, without overreaching: multi-source routes are common (1056 of 5631 unique routes) and their reported temperatures vary often enough that per-purpose conflict handling (`apply_condition_precedents`, Phase 19, `src/process.rs`) is not addressing a hypothetical problem -- but the actual per-purpose conflict rate remains unmeasured, since this corpus has no purpose labels to measure it against.

## Applicability to a bulk `ProcessEvidenceProvider`

- Structurally valid **and** temperature-resolved (usable to directly seed a `ConditionPrecedent`, the same bar Phase 10's curated records meet): 7227 (79.9% of valid records).
- Structurally valid but not temperature-resolved (usable only as a free-text evidence pointer, not an automatic condition source -- every valid record has a DOI, so this is simply the complement of the count above, not a separately DOI-gated figure): 1818.

## What this audit does not establish

- Extraction accuracy: this dataset is itself the output of an NLP text-mining pipeline (Kononova et al. 2019's own), not hand-verified by gugen. This audit measures the shape and coverage of what that pipeline extracted, not whether the extracted numbers are correct against the original papers.
- Whether a bulk provider built on this corpus would improve gugen's actual planning outputs -- that requires the provider to exist and be benchmarked, which this report deliberately does not build.
- Anything about the Mechanochemical route family, gas-phase reactions, or thermodynamic selectivity -- out of scope for this audit.

